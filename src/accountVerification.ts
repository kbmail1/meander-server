// Fan 30 / 31

// --- Sign in
// send email - nodemailer.
// gen 4 digit number, save it against the email. send email. have user enter it to validate.
// if yes, then that email address ID user id
//
// https
// send back plain text password, server saves as hash.
//
// when user logs in, if email exists, good.  else prompt to sign-up.
// use mongo to store creds (email, password, id, display_name)

// ------ DB
// install mongo and ndoejs mongoose driver
// create user schema above.
// implement test CRUD

// ------ UI
// Sign up, Login, enter email code - and yes, id created => related pages.
// ... ensure responsive and A11y