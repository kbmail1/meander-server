/*
import React, { useEffect } from 'react'
import { gql, useQuery } from '@apollo/client'

const GET_USERS = gql`
  query GetDogs {
    sayHello {
      id
      name
    }
  }
`
const App = () => {
  const { loading, error, data } = useQuery(GET_USERS)
  return (
    <>
      <h1>{loading ? 'Loading' : error ? 'Failed' : data?.sayHello?.name}</h1>
    </>
  )
}
*/

export default App
